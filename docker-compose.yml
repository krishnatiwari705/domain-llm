services:
  api:
    build: .
    ports: ["8000:8000"]
    volumes:
      - ./data:/app/data:ro
      - ./artifacts:/app/artifacts:ro
    environment:
      ADAPTER_PATH: /app/artifacts/lora
      LABELS_PATH: /app/data/processed/labels.json
